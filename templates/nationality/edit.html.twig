{% include 'default/_form.html.twig' with {
    page_title: 'title.nationality_edit'|trans({'%id%': nationality.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'nationality_index',
} %}