{% include 'default/_form.html.twig' with {
    page_title: 'title.nationality_delete'|trans({'%name%': nationality.name|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'nationality_index',
} %}
