{% include 'default/_form.html.twig' with {
    page_title: 'title.input_delete'|trans({'%title%': input.title|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'input_index',
} %}
