{% include 'default/_form.html.twig' with {
    page_title: 'title.movement_delete'|trans({'%name%': movement.name|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'movement_index',
} %}
